%section.single
  %h1= @entry.title
  %div= simple_format(hbr(@entry.content))
  %time
    = time_ago_in_words(@entry.created_at)
    前
  %p.helpUp
    Written by ブラザー&nbsp;
    %strong= @entry.brother.name