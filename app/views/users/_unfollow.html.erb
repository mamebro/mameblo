<%= form_for(current_user.relationships.find_by_followed_id(@user),
    html: { method: :delete },
    remote: true) do |f| %>
<%= f.submit "Un ブラザー", class: "button red", id: 'unBrother' %>
<input type="button" id="unBrotherAfter" class="button gray" value="ブラザーをやめました">
<% end %>
