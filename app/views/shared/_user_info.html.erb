<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 52 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "‚ä[‚´[", current_user %>
</span>
<span>
  <%= pluralize(current_user.entries.count, "entry") %>
</span>
