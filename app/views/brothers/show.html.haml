= provide(:title, @brother.name)
%h1.brother-name
  = @brother.name
= render 'shared/stats'
= render 'follow_form' if signed_in?
- if @brother.entries.any?
  %article#entries
    = render @entries
  = paginate @entries