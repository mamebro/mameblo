= provide(:title, 'みんな')
%h1.page-title
  ??? 兄弟かも ???
%div#content
  %ul.cards.autopager-content
    - @brothers.each do |brother|
      %div.card
        %a.card-content{href: "/brothers/#{brother.name}"}
          = brother.name
